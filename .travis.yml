language: ruby
env: 
  - RAILS_ENV=test
before_script:
  - psql -c 'create database gtbox_account_test;' -U postgres
  - rake db:migrate
  
install:
  - yarn install
  - bundle install --jobs=3 --retry=3

services:
  - postgresql
